/// Represents the keys of the virtual Pad in the game
///
/// - up: The Up key in the Pad
/// - : the other keys
public enum GamePadKey : Int {
    case up = 0,
    down,
    left,
    right,
    x,
    triangle,
    square,
    circle,
    
    numOfKeys;
}
