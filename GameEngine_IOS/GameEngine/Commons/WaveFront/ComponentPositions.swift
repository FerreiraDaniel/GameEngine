import Foundation

/**
 *	Positions where the components of the color are when each line of wavefront
 *is parsed
 */
open class ComponentPositions {
    open static let r : Int = 1;
    open static let g : Int = 2;
    open static let b : Int = 3;
}
