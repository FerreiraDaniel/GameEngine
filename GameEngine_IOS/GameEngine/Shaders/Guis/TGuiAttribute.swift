import Foundation

/**
 * Enumeration of attributes to the GUI shader
 */
public enum TGuiAttribute : Int {
    case
    
    /**
     * Position where to location attribute is going to be bind in the shader
     * program
     */
    position = 0,
    
    
    /**
     * Number of entity attributes
     */
    numOfAttributes;
}
